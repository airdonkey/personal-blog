# 网站后台修改指南

这套后台的目标是：日常修改任何作者可控的可见文字，都不需要打开代码文件，也不需要理解 Hugo、TOML 或 Git。

## 最短操作路径

1. 打开 [Pages CMS](https://app.pagescms.org/) 并用 GitHub 登录。
2. 进入 `airdonkey/personal-blog`，确认正在编辑准备发布的分支。
3. 根据要改的内容，从左侧进入对应入口。
4. 一次只改一处，保存。
5. 查看提交差异；确认后再合并到 `main`。Cloudflare Pages 会自动重新构建网站。

如果只是改一个错字，不需要在本地运行任何命令。

## 正文编辑器：像普通文档一样使用

主文、来时路、小径和游记的“正文”已经移到标题、日期、语言和网址短名之后，不必先填完所有发布信息才开始写。正文默认只显示所见即所得编辑器。

- **输入与粘贴文字：**直接输入，或从 Word、网页、备忘录复制后粘贴。选中文字时会出现加粗、斜体、链接等格式菜单。
- **插入图片：**把光标放在空白行，输入 `/` 后选择 Image；也可以直接把图片文件粘贴到正文或拖入正文。上传完成后，文章图片会自动保存到 `static/images/articles`，正文自动写入 `/images/articles/...`；游记图片则自动进入 `static/images/travel`。
- **插入表格：**在空白行输入 `/`，选择 Table。表格数据之后仍可在编辑器中修改。
- **插入数据图表：**先在 Excel、Google Sheets、Numbers 或其他制图工具中复制或导出图表，再像图片一样粘贴；这样网站仍是轻量静态页面，不需要加载一套图表运行库。
- **图片说明：**插入后点击图片，补上简短的替代文字（Alt text），说明图中真正重要的内容。

图片正在上传时先不要点击 Save；等临时预览变成正式图片后再保存。如果看不到新编辑器，刷新 Pages CMS 页面后重新打开文章。

## 左侧入口如何选择

| 想修改什么 | 后台入口 |
|---|---|
| 首页作者名、网站标题、身份短句、搜索与分享摘要、主标题、信念短句、开场简介、联系邮箱 | `00 网站可见文字 → 01 网站身份与首页主文案` |
| “文章”“来时路”“小径”“旅程档案”“话题索引”的栏目标题、简介和导言 | `00 网站可见文字 → 02 栏目标题与导言` |
| 四个长期母题的名称、说明、问题和追问角度 | `00 网站可见文字 → 03 母题、人生阶段与标签 → 长期母题` |
| 来时路六个人生阶段的名称和说明 | `00 网站可见文字 → 03 母题、人生阶段与标签 → 来时路的人生阶段` |
| 标签的公开名称和说明 | `00 网站可见文字 → 03 母题、人生阶段与标签 → 话题标签词表` |
| Grand Tour 的国家、城市、地点、到访时间和双语说明 | `00 网站可见文字 → 04 Grand Tour 国家、城市与地点` |
| 导航、按钮、工作稿提示、地图提示、页脚等短文字 | `00 网站可见文字 → 05 中文按钮、提示语与导航`；英文在 `06` |
| 关于页、实践页、主题页、联系页和 Grand Tour 总说明 | `固定页面 / Core pages` |
| 某篇主文，以及它的 1200×630 社交分享图路径 | `主文（完整论述）/ Essays` |
| 某篇自传体回忆 | `来时路（个人生活史）/ Memoir` |
| 某篇短札记 | `小径（短札记）/ Side Paths` |
| 某段游记、路线、主图、相册和图片说明 | `旅程与照片故事 / Journeys and photo stories` |

## 三条防误操作规则

1. 标有“稳定标识（请勿修改）”的字段已经锁定。它决定文章关联、锚点或网址，不是给读者看的标题。
2. 高级界面短语中如果出现 `{{ .Count }}` 一类占位符，只改它周围的文字，原样保留占位符。
3. 修改 Grand Tour 地图时，地点名称和说明可以随时改；经纬度、国家代码和区域会影响地图定位或归组，不确定时先不动。

## ADHD 友好的默认流程

每次只完成一个闭环：

1. 选中一个页面或一条短语。
2. 只改当前最碍眼的一处。
3. 保存并写一句提交说明。
4. 关闭后台，不顺手重构别的栏目。

栏目结构、稳定键和网址不属于日常编辑。它们只在季度复盘时集中评估。

## 哪些内容不是手写文案

- 年份、阅读时间、文章数量、国家数量等由内容数据自动计算。
- OpenStreetMap attribution 是地图服务要求保留的第三方署名。
- 关闭、上一张和下一张的符号按钮属于交互控件。

除此以外，作者控制的中英文正文、标题、栏目说明、标签说明、导航与界面提示都已经有后台入口。
